var markup = document.getElementById('home');
var btn = document.querySelector('.btn');

const vlookup = document.querySelectorAll();

btn.addEventListener('click', function() {
    console.log('heelloooooo');
})

